function solution(n, words) {
    let answer = [];
    let prev = [];
    let index = 0;
    words.reduce((bef, cur, i) => {
        
    })
    if(answer[0] === undefined) {
        return [0, 0]
    }

    return answer
}


console.log(solution(3, ["tank", "kick", "know", "wheel", "land", "dream", "mother", "robot", "tank"]));